spring:
  application:
    name: tourism-hotel-booking-monolith

  datasource:
    url: jdbc:mysql://localhost:3306/hotel_db?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  main:
    allow-bean-definition-overriding: true

server:
  port: 8080

app:
  jwtSecret: GchPX4bzotRhYBArjDkR2FHmokpgB0psVOC2RNPujkQ=
  jwtExpirationMs: 86400000